<odoo>
    <!-- Define a sequência primeiro -->
    <record id="seq_repair_order_mrp" model="ir.sequence">
        <field name="name">Sequência OS MRP</field>
        <field name="code">repair.order.seq</field>
        <field name="prefix">OS/</field>
        <field name="padding">5</field>
    </record>

    <!-- Herança da View de Formulário -->
    <record id="view_repair_order_form_inherit_mrp_final" model="ir.ui.view">
        <field name="name">repair.order.form.inherit.mrp</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.repair_order_form"/>
        <field name="arch" type="xml">
            <!-- Injeta campos básicos para testar se a herança funciona -->
            <xpath expr="//field[@name='product_id']" position="after">
                <field name="os_number" readonly="1"/>
            </xpath>
            
            <xpath expr="//notebook" position="inside">
                <page string="MOs Vinculadas" name="mo_page">
                    <field name="production_ids"/>
                </page>
            </xpath>
            
            <xpath expr="//header" position="inside">
                <button name="action_export_report" type="object" string="Excel" class="btn-secondary"/>
            </xpath>
        </field>
    </record>
</odoo>
